# ==========================================================
# DATABASE CONFIGURATION
# Matches: docker-compose -> postgres (service name: postgres)
# ==========================================================
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql://postgres:changeme@postgres:5432/ai_vault

# ==========================================================
# REDIS CONFIGURATION
# Matches: docker-compose -> redis (service name: redis)
# ==========================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379
RATE_LIMIT_REDIS_URL=redis://redis:6379

# ==========================================================
# SUPABASE CONFIGURATION (local simulation or real project)
# Matches: Supabase Studio -> postgres service
# ==========================================================
SUPABASE_URL=http://postgres:5432
SUPABASE_ANON_KEY=dev-local-anon-key

# ==========================================================
# JWT CONFIGURATION (Authentication / Access Control)
# Used by: Vault service, Trust engine
# ==========================================================
JWT_SECRET=uIvd9h8K2mN5pQ7sT0wZ3y6B9eR2fU5hK8mP1qS4vX7zA0cF3gJ6lO9rT2wY5bE8
ACCESS_TTL=900          # 15 minutes
REFRESH_TTL=604800      # 7 days

# ==========================================================
# PRIVACY ENGINE + MONITORING PARAMETERS
# Used by: BudgetMonitor, PrivacyService, AlertSystem
# ==========================================================
TRUST_SCORE_TTL=900
RATE_LIMIT_PER_MINUTE=100

# ==========================================================
# SECURITY SETTINGS
# Shared secret used for FastAPI sessions, encryption, etc.
# ==========================================================
SECRET_KEY=supersecret_long_random_string

# ==========================================================
# CORS (Frontend origin for browser calls)
# Matches: localhost:3000 if using Vite/Next.js frontend
# ==========================================================
ALLOWED_ORIGINS=http://localhost:3000
